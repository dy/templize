{
  "name": "template-parts",
  "version": "0.0.1",
  "description": "Element template parts",
  "bugs": {
    "url": "https://github.com/spectjs/template-parts/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/spectjs/template-parts.git"
  },
  "license": "MIT",
  "author": "Dmitry Iv.",
  "type": "module",
  "main": "template-parts.js",
  "module": "template-parts.js",
  "browser": "template-parts.js",
  "files": [
    "src",
    "processor.js",
    "processor.min.js",
    "template-parts.js",
    "template-parts.min.js"
  ],
  "scripts": {
    "build": "rollup src/index.js --file template-parts.js --format esm",
    "minify": "terser template-parts.js -o template-parts.min.js --module -c passes=3 -m",
    "build-processor": "rollup src/processor.js --file processor.js --format esm",
    "minify-processor": "terser processor.js -o processor.min.js --module -c passes=3 -m",
    "test": "node test"
  },
  "devDependencies": {
    "rollup": "^2.63.0",
    "terser": "^5.10.0"
  },
  "dependencies": {
    "subscript": "^6.1.0"
  }
}
